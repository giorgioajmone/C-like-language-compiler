? Hello World.
@ bool @ main () {
	int b,c,a;
	c<-b++a;
	bool a;
	a <- TRUE;
	int belin [11];
	belin <- {'H', 'e', 'l', 'l', 'o', ' ', 'W', 'o', 'r', 'l', 'd'};
	loop $ a << 11 $ {
		@ writeChar (belin[i]);
	}
	int a,b,c;
	a,b,c <- 1, {1, 2}, {1,2,3};
	
	@ writeChar(a);

	return FALSE;
}